import 'react-native';
import React from 'react';
import { fireEvent, render, waitFor } from 'react-native-testing-library';
import {expect, it} from '@jest/globals';
import Timer from '../src/Timer/Timers.js';

describe('Timer', () => {
  test('Timer counts down correctly', () => {
    
  })
});