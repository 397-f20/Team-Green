import React from 'react';
import renderer from 'react-test-renderer';

import App from '../../App';

describe.skip('<App />', () => {
  it('should pass', () => {
      expect(1).toEqual(1);
    
  });
});